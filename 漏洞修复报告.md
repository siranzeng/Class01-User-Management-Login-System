# 漏洞修复报告 - 用户信息管理平台

| 项目 | 内容 |
|------|------|
| 项目名称 | Class01-User-Management-Login-System |
| 修复版本 | v2.1 |
| 修复日期 | 2026-07-20 |
| 修复人员 | 曾思填 |

---

## 修复问题清单

### 1. SQL 注入漏洞 (高危)

**问题:** 注册和搜索功能中用 f-string 拼接 SQL, 用户输入没做任何过滤。

**漏洞代码 (修复前):**
```python
# 搜索 - 字符串拼接
sql = f"SELECT ... WHERE username LIKE '%{keyword}%' OR email LIKE '%{keyword}%'"
c.execute(sql)

# 注册 - 字符串拼接
sql = f"INSERT INTO users ... VALUES ('{username}', '{password}', '{email}', '{phone}')"
c.execute(sql)
```

**修复:** 改用参数化查询, 用户输入通过 ? 占位符传入, 不参与 SQL 语句拼接。
```python
# 搜索 - 参数化
sql = "SELECT ... WHERE username LIKE ? OR email LIKE ?"
c.execute(sql, (f"%{keyword}%", f"%{keyword}%"))

# 注册 - 参数化
sql = "INSERT INTO users ... VALUES (?, ?, ?, ?)"
c.execute(sql, (username, password, email, phone))
```

**POC 验证结果:**
| 测试项 | 修复前 | 修复后 |
|--------|--------|--------|
| UNION注入 | 返回自定义数据 inj | 无搜索结果 |
| OR万能注入 | 返回所有用户 | 无搜索结果 |
| 注册注入 | SQL注入成功 | 字符串被当作用户名原样存储 |

### 2. 之前已修复 (v2.0)

| 漏洞 | 等级 | 修复措施 |
|------|------|----------|
| 明文密码存储 | 高危 | PBKDF2+SHA256 哈希 |
| 无 CSRF 防护 | 高危 | 一次性 Token |
| 暴力破解无防护 | 高危 | IP限流+账户锁定+指数退避 |
| 密码明文展示 | 中危 | 改为 ****** |
| 调试账号泄露 | 中危 | 注释只提用户名 |
| SECRET_KEY 硬编码 | 中危 | secrets.token_hex(32) |

## 修改的文件

| 文件 | 变更 |
|------|------|
| app.py | SQL改为参数化查询, 新增注册/搜索路由 |
| templates/register.html | 新增注册页面 |
| templates/index.html | 新增搜索功能 |
| templates/base.html | 导航栏加注册链接 |
| templates/login.html | 加成功提示 |
| static/css/style.css | 加搜索表格等样式 |
